spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL
    username: sa
    password:

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        #format_sql: true
    open-in-view: false

  mail:
    host: smtp.naver.com
    port: 465
    username: t3231@naver.com
    password: 12341234
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: smtp.naver.com
          starttls:
            enable: true
            required: true

  cloud:
    aws:
      s3:
        bucket: test-bucket
        endpoint: http://127.0.0.1:8001
      region:
        static: ap-northeast-2
      credentials:
        access-key: fu
        secret-key: ba